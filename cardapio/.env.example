# Base pública dos snapshots do cardápio (sem barra no fim).
# Dev: MinIO do docker-compose. Produção: domínio do CDN, configurado nas env vars do Vercel.
# Copie para .env.local (ignorado pelo git).
VITE_SNAPSHOT_BASE_URL=http://127.0.0.1:9000/la-carte
